# Byte Bloom — Demo (single-machine)

This repository includes a demo backend and a single-file frontend for a cloud misconfiguration scanner.

## Setup (local dev)

1. Create virtualenv & install backend deps:
```bash
python -m venv .venv
# Windows
.venv\\Scripts\\activate
# mac/linux
source .venv/bin/activate

pip install -r requirements.txt
